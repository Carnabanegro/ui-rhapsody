import { describe, it, expect } from 'vitest';
import { UiButtonComponent } from '@ui-rhapsody/core';

describe('UiButtonComponent', () => {
  it('should be defined', () => {
    expect(UiButtonComponent).toBeDefined();
  });
});