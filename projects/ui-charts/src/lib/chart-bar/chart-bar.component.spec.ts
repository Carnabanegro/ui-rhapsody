import { describe, it, expect } from 'vitest';
import { UiBarChartComponent } from './chart-bar.component';
describe('UiBarChartComponent', () => { it('should be defined', () => { expect(UiBarChartComponent).toBeDefined(); }); });