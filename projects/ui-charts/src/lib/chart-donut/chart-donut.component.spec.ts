import { describe, it, expect } from 'vitest';
import { UiDonutChartComponent } from './chart-donut.component';
describe('UiDonutChartComponent', () => { it('should be defined', () => { expect(UiDonutChartComponent).toBeDefined(); }); });