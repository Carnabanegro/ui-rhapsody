import { describe, it, expect } from 'vitest';
import { UiHeatmapChartComponent } from './chart-heatmap.component';
describe('UiHeatmapChartComponent', () => { it('should be defined', () => { expect(UiHeatmapChartComponent).toBeDefined(); }); });