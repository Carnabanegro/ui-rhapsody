import { describe, it, expect } from 'vitest';
import { UiTreemapChartComponent } from './chart-treemap.component';
describe('UiTreemapChartComponent', () => { it('should be defined', () => { expect(UiTreemapChartComponent).toBeDefined(); }); });