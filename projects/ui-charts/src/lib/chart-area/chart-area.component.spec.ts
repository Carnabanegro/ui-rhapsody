import { describe, it, expect } from 'vitest';
import { UiAreaChartComponent } from './chart-area.component';
describe('UiAreaChartComponent', () => { it('should be defined', () => { expect(UiAreaChartComponent).toBeDefined(); }); });