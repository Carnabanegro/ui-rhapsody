import { describe, it, expect } from 'vitest';
import { UiLineChartComponent } from '@ui-rhapsody/charts';

describe('UiLineChartComponent', () => {
  it('should export UiLineChartComponent', () => {
    expect(UiLineChartComponent).toBeDefined();
  });

  it('should have chartReady output', () => {
    expect(UiLineChartComponent).toBeDefined();
  });
});