import { describe, it, expect } from 'vitest';
import { UiScatterChartComponent } from './chart-scatter.component';
describe('UiScatterChartComponent', () => { it('should be defined', () => { expect(UiScatterChartComponent).toBeDefined(); }); });